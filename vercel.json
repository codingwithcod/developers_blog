{
  "version": 2,
  "framework": null,
  "installCommand": "cd backend && yarn install && cd ../frontend && yarn install",
  "buildCommand": "cd backend && yarn build && cd ../frontend && yarn build",
  "outputDirectory": "frontend/.next",
  "functions": {
    "api/index.js": {
      "includeFiles": "backend/dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/v1/(.*)",
      "destination": "/api/index"
    }
  ]
}
